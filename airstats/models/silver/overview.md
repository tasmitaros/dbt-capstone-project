# Silver Layer Overview

## Tables

The silver layer contains three connected tables that provide clean data that is ready for analysis.

### silver_airports
The core dimension table containing airport information including identifiers, names, types, adn geographic coordinates. This is the parent table that other tables reference.